name: Deploy Flask Backend to Azure Web App

on:
  push:
    branches:
      - main  # Change if your main branch is named differently

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # Run database migrations if using Flask-Migrate
      - name: Run DB migrations
        env:
          FLASK_APP: app.py  # change to your Flask entrypoint
          FLASK_ENV: production
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          flask db upgrade || echo "No migrations to run"

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'mealbuddyfinalproject' # Azure Web App name
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE }}
